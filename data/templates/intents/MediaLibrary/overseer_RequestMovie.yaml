---
intents:
  request-movie:
    data:
      - sentences:
          - "<download> [the] <movie> {movie_title}"
          - "<download> {movie_title}"
          - "<download> [the] <newness> {movie_title}"