---

intents:
  search-media:
    data:
      - sentences:
        - "<search> for {genre} {media_type}"
        - "<search> {media_type} <starring> {actor}"
        - "<search> {media_type} directed by {actor}"
        - "<search> for the <newness> {movie_title}"
        