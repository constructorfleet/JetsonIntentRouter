---

intents:
  play-media:
    data:
      # Play a specific media title on a device
      - sentences:
          - "<play> {movie_title} <in> {media_player}"
          - "<play> {show_title} <in> {media_player}"
          - "<play> {episode_name} <in> {media_player}"
          - "<play> [the show] {show_title} season {season} episode {episode} <in> {media_player}"
          - "<play> [the episode] {episode_name} <in> {media_player}"
