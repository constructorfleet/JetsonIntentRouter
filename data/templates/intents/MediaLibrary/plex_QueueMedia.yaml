---

intents:
  queue-media:
    data:
      # Queue a specific media title to play next on a device
      - sentences:
          - "<queue> {movie_title} <in> {media_player}"
          - "<queue> {show_title} <in> {media_player}"
          - "<queue> {episode_name} <in> {media_player}"
          - "<queue> [the] <show> {show_title} season {season} episode {episode} <in> {media_player}"
          - "<queue> [the] <episode> {episode_name} <in> {media_player}"
          - "<queue> {movie_title} <in> {media_player}"
          - "<queue> {show_title} <in> {media_player}"
          - "<queue> {episode_name} <in> {media_player}"
          - "<queue> [the show] {show_title} season {season} <episode> {episode} <in> {media_player}"
          - "<queue> [the episode] {episode_name} <in> {media_player}"